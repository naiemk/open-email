FROM node:22-bookworm-slim
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev
COPY client ./client
COPY relayer ./relayer
COPY node ./node
COPY dal ./dal
ENV NODE_ENV=production
EXPOSE 80 25
CMD ["node", "--experimental-strip-types", "--no-warnings", "node/src/main.ts"]
