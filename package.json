{
  "name": "open-email",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run && (cd registry && forge test)",
    "test:client": "vitest run client",
    "test:relayer": "vitest run relayer",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/ciphers": "^2.4.0",
    "@noble/curves": "^2.4.0",
    "@noble/hashes": "^2.4.0",
    "viem": "^2.56.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
